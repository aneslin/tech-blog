<article>
    <div class="card">
        <div class="card-body">
            <div class="card-title">
               <h3> {{post.post_title}}</h3>
            </div>
            <div class="container">
                <div class="row">
                <div class="card-text col-10">
                    {{post.post_content}}
                    </div>
                   <div class="card-text col-2">
                       {{post.user.username}}
                   </div>

                </div>
            </div>
        </div>
    </div>

    </div>

</article>